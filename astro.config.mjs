// @ts-check
import { defineConfig } from "astro/config";

// When the site is live, put your real address here (used for SEO and share links).
export default defineConfig({
  site: "https://amiralalisarvestani-portfolio.netlify.app",
});
