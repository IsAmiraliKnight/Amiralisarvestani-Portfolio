# راهنمای پورتفولیو

## اجرای سایت روی کامپیوتر خودت

یک بار (فقط دفعه اول):

```bash
npm install
```

هر بار که می‌خوای سایت رو ببینی:

```bash
npm run dev
```

بعد توی مرورگر برو به `http://localhost:4321`. هر تغییری بدی، سایت خودکار به‌روز می‌شه.

---

## تغییر اطلاعات شخصی (اسم، ایمیل، متن‌ها)

همه‌اش توی یک فایله: `src/data/site.json`

| فیلد | چیه |
|---|---|
| `name` | اسم کامل |
| `shortName` | اسمی که جای لوگو و اسم بزرگ پایین سایت میاد |
| `role` | تیتر بزرگ صفحه اول (UI/UX Designer) |
| `eyebrow` | جمله کوچیک بالای تیتر |
| `email` | ایمیل (توی هدر و بخش تماس) |
| `bookCallUrl` | لینک دکمه Book a call (مثلاً لینک Calendly) |
| `socials` | لینک شبکه‌های اجتماعی |
| `about` | متن‌های بخش درباره من |
| `contact` | متن‌های بخش ارتباط با من |

رزومه: فایل PDF خودت رو با اسم `resume.pdf` بذار توی پوشه `public` (جایگزین فایل فعلی).

---

## اضافه کردن یک پروژه جدید

1. برو توی پوشه `src/content/projects`
2. یکی از پوشه‌های نمونه (مثلاً `nova-banking`) رو **کپی** کن و اسمش رو عوض کن، مثلاً `my-new-app`
   - اسم پوشه می‌شه آدرس صفحه: `yoursite.com/projects/my-new-app`
   - فقط حروف انگلیسی کوچیک، عدد و خط تیره (`-`)
3. عکس‌های قبلی رو پاک کن و عکس‌های خودت رو بذار توی همون پوشه (jpg, png, webp)
4. فایل `index.md` رو باز کن و بالاش رو پر کن:

```yaml
---
title: My New App            # اسم پروژه
year: "2026"                 # سال
badge: NEW                   # (اختیاری) برچسب کنار سال — اگه نمی‌خوای این خط رو پاک کن
summary: یک جمله کوتاه درباره پروژه
cover: ./cover.jpg           # عکس اصلی صفحه پروژه
gallery: [./01.jpg, ./02.jpg, ./03.jpg]   # عکس‌های اسلایدر و شبکه عکس صفحه اول
role: Lead UI/UX Designer    # (اختیاری)
duration: 3 months           # (اختیاری)
tools: [Figma, Protopie]     # (اختیاری)
team: 2 designers            # (اختیاری)
order: 1                     # ترتیب نمایش: عدد کمتر = بالاتر
---
```

5. زیر خط `---` دوم، متن Case Study رو بنویس:

```md
## The challenge

متن معمولی...

![توضیح عکس](./02.jpg)

- یک نکته
- یک نکته **پررنگ**
```

- `##` = تیتر بخش
- `![...](./اسم-عکس.jpg)` = عکس تمام‌عرض
- برای ویدیو یا پروتوتایپ Figma می‌تونی کد `<iframe ...>` رو مستقیم بذاری

نکته‌ها:
- بهترین نسبت عکس‌ها برای اسلایدر **16:10** هست (مثلاً 1600×1000)
- اگه پروژه‌ای هنوز آماده نیست، `draft: true` بذار تا نمایش داده نشه
- برای حذف یک پروژه، کل پوشه‌اش رو پاک کن

---

## انتشار سایت (Deploy) — Netlify

### روش ۱: ساده‌ترین (بدون GitHub)
1. `npm run build` رو اجرا کن، یه پوشه `dist` ساخته می‌شه
2. برو به [app.netlify.com/drop](https://app.netlify.com/drop)
3. پوشه `dist` رو بکش و رها کن توی صفحه. تمام!
4. برای به‌روزرسانی، دوباره build بگیر و پوشه جدید رو بکش توی بخش Deploys همون سایت

### روش ۲: خودکار با GitHub (پیشنهادی)
1. پروژه رو روی GitHub آپلود کن
2. توی Netlify: **Add new site → Import from Git** و ریپازیتوری رو انتخاب کن
3. تنظیمات رو خودش تشخیص می‌ده (Build command: `npm run build`، Publish directory: `dist`)
4. از این به بعد هر تغییری push کنی، سایت خودکار به‌روز می‌شه

بعد از انتشار، آدرس نهایی سایت رو توی فایل `astro.config.mjs` جای `https://example.com` بذار.

---

## جاهایی که بعداً باید عوض بشن (Placeholder)

- **لوگو هدر**: `src/components/Header.astro` (کامنت LOGO PLACEHOLDER)
- **لوگوی متحرک بخش تماس**: `src/components/Contact.astro` (کامنت LOGO MARK PLACEHOLDER)
- **ایمیل و لینک‌ها**: `src/data/site.json`
- **رزومه**: `public/resume.pdf`
- **پروژه‌های نمونه**: سه پوشه داخل `src/content/projects`
